import React from 'react';

const functionPane = (props) => (
    <div>
        <p>sin cos tan ln sum integral deriv</p>
        <p>( ) , ^ sqr root</p>
        <p>    {'= > <'}  </p>
    </div>
);

export default functionPane;
